// Unversioned alias of /api/v1/search, kept for clients using the older path.
// Route Handlers are dynamic by default, so no segment config is needed here.
export { GET } from '../v1/search/route';
